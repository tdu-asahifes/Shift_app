# シフト・出退勤管理システム 要件定義

**学園祭実行委員向け　Version 5.0**

---

## ■ 概要

学園祭実行委員の準備日・当日における出退勤管理・シフト確認・連絡共有をシンプルに行うことを目的とする。

---

## ■ 出退勤管理機能

### ● QRコード打刻

- 各担当場所ごとにQRコードを設置
- 読み取り後、打刻ページへ遷移
- URL例：`https://example.com/check?location=受付`

### ● 打刻情報

| フィールド | 内容 |
|---|---|
| 学籍番号 | ログイン中のユーザー |
| 場所ID | URLパラメータから取得 |
| 打刻種別 | 出勤 / 自動退勤 |
| 時刻 | 自動取得 |

### ● 打刻方法

- 「出勤」ボタンのみ表示（退勤ボタンは廃止）

### ● 重複打刻の扱い

- **同じ場所への2回目以降の出勤打刻は無視する**（上書き・追記しない）
- ただし、シフトの途中で場所や業務が変わる場合は、新しい場所のQRを読んで都度打刻する運用とする
- 別の場所への打刻は「自動退勤」として処理する（後述）

### ● 打刻完了通知

- 打刻後に「打刻完了」メッセージを画面に表示
- 表示内容：出勤・時刻・場所
- 打刻完了後、同じ場所のメンバー出勤状況を同一画面に表示する（出勤状況確認機能と統合）

---

## ■ 自動退勤仕様

退勤打刻は廃止し、以下のルールでバックグラウンド処理する。

- スタッフが**別の場所**で出勤打刻した場合、前の場所の出勤レコードを自動退勤扱いに更新する
- 自動退勤時刻 = 新しい場所への出勤時刻
- スタッフへの通知なし・画面表示なし（サイレント処理）
- DBのレコード更新のみで完結

### ● 一括退勤処理（終了時刻）

- **毎日18:30に、その日出勤中（退勤未記録）の全レコードを自動退勤扱いに更新する**
- 夜通し作業は想定しないため、18:30以降は全員退勤済みとみなす
- バッチ処理または GAS のタイマートリガーで実装

---

## ■ ログイン機能（簡易）

### ● 入力項目

- 学籍番号
- 当日のログインコード

### ● 認証ロジック

1. 管理者が設定した「当日のコード」と入力コードが一致するか確認
2. 入力された学籍番号が当日のシフトデータに存在するか確認
3. 両方一致 → ログイン成功
4. **学籍番号がシフトデータに存在しない → ログイン拒否**（エラーメッセージ表示）

### ● セッション管理

- ローカルストレージに保存
- **同じ日中はログイン状態を維持する（QRを読むたびの再ログインは不要）**
- 日付が変わったら再ログイン必須
- 注意：プライベートモードやブラウザのキャッシュ削除でセッションが消える場合があるため、運用時にスタッフへ周知する

---

## ■ 場所外打刻の処理（迷子ログ）

スタッフが自分のシフトと異なる場所のQRを読んだ場合の挙動。

### ● 画面表示

```
この場所のシフトではありません。
あなたの担当場所は「〇〇」です。
```

- 出勤記録はしない（正規の打刻として扱わない）

### ● 裏側の保存（迷子ログ）

- **別テーブル（または専用フラグ）に「場所外打刻」として保存する**
- 保存内容：学籍番号・スキャンした場所ID・時刻
- 目的：「あの人どこ行った？」となったときに管理者が確認できるようにする
- スタッフには見せない（管理者向け情報）

---

## ■ 出退勤画面表示

- 担当場所ごとのマニュアル表示（打刻画面内に表示）
- 当日の連絡事項表示（打刻画面内に表示）

---

## ■ 打刻履歴機能

### ● 表示内容

- 自分の当日の打刻履歴（出勤・自動退勤）
- 表示項目：打刻種別・時刻・場所・自動退勤の場合は移動先も表示

### ● 仕様

- 学籍番号でフィルタ
- 当日のデータのみ表示（初期）
- シンプルなリスト形式

---

## ■ 出勤状況確認機能

現場に到着した際に、同じ場所に配置された他のメンバーの出勤状況を確認できる機能。引継ぎの安定化が目的。

### ● 表示対象（メンバーの範囲）

- **同じ場所（場所ID）に配置されているメンバー全員（時間帯問わず）**
- 前後の時間帯のメンバーも含めることで、引継ぎ相手が来ているか確認できる
- 例：「受付」に10時・13時・16時シフトの人がいれば全員表示

### ● 表示内容

| 項目 | 内容 |
|---|---|
| 名前または学籍番号 | GASのシフトデータから取得 |
| シフト時間帯 | いつのシフトの人か分かるように表示 |
| 出勤状況 | 未出勤 / 出勤中 / 退勤済（自動退勤・18:30一括含む） |
| 出勤時刻 | 出勤済みの場合のみ表示 |
| 直前の勤務場所と状況 | 後述。未出勤メンバーの居場所推測に使う |

### ● 出勤状況の色分け

| 状態 | 表示色 |
|---|---|
| 出勤中 | 緑 |
| 退勤済 | トーンを落とした緑（済みであることを示す） |
| 未出勤 | グレー |

### ● 直前の勤務履歴表示

メンバーが未出勤の場合に「直前にどこにいたか」を手がかりとして表示する。

- 表示件数：**直前の1件のみ**（今どこにいそうか推測できれば十分）
- 表示内容：直前の場所名・出勤〜退勤時刻
- 表示条件：打刻レコードが存在する場合のみ表示（初日・初シフトで記録がない場合は表示しない）
- 例：「前の勤務：受付　9:00〜12:00」

### ● 閲覧権限

| 対象 | 閲覧範囲 |
|---|---|
| 一般スタッフ | 同じ場所に配置されたメンバーの直前1件のみ閲覧可能 |
| リーダー・管理者 | 全メンバー・全シフトの履歴を閲覧可能（管理者機能として今後実装） |

- 一般スタッフは別の場所のメンバー情報は閲覧不可
- 権限チェック：ログイン中の学籍番号とGASのシフトデータで判定

### ● 表示タイミング

- 出勤打刻完了後の画面に表示（打刻完了メッセージと同一画面）
- ページ再読み込みで状態を更新（リアルタイム更新は今後の拡張）

---

## ■ シフト一覧画面

ユーザーが自分の一日のシフト全体を確認できる画面。

### ● タイムライン（帯グラフ）

- 横軸を時間軸（1時間ごとに目盛り）とした帯グラフを表示
- 各シフトを横帯として表示し、時間の長さに比例した幅で描画
- 現在時刻を赤い破線で示す
- 複数シフトが重なる場合は縦に並べて表示

### ● 帯の色分け

| 状態 | 表示色 |
|---|---|
| 出勤中（現在） | 緑（濃） |
| 退勤済 | 緑（薄・トーンダウン） |
| 未出勤（今後） | グレー |

### ● シフト詳細リスト

- タイムラインの下にシフトをカード形式で一覧表示
- 各カードの表示内容：場所名・時間帯・出勤状態バッジ・連絡事項
- 現在出勤中のシフトカードは枠線を強調表示

---

## ■ 打刻画面（詳細）

### ● 表示内容

- 現在地（場所名）・日時
- ログイン中のユーザー名・学籍番号
- 当日の連絡事項
- **現在のシフト**（場所・時間帯・出勤状態）
- **次のシフト**（場所・時間帯・次のシフトまでの残り時間）
- 出勤ボタン

### ● 次のシフト表示の仕様

- GASのシフトデータから、当日の自分のシフトを時系列で取得し、現在時刻より後の直近シフトを「次のシフト」として表示
- 次のシフトがない場合は表示しない
- 「次の場所のQRで打刻すると自動的に移動します」という案内文を添える

---

## ■ 画面構成・ナビゲーション

### ● ボトムナビゲーション（3タブ）

| タブ | 内容 |
|---|---|
| 打刻 | QR読み取り後の打刻画面 |
| シフト | 一日のシフト一覧（タイムライン＋カードリスト） |
| 履歴 | 自分の当日打刻履歴 |

---

## ■ シフト管理（GAS連携）

### ● データ管理

Googleスプレッドシートを使用。シートは以下の構成とする。

**① シフトシート（メインデータ）**

| 日付 | 時間 | 名前 | 学籍番号 | 場所 | 連絡 |
|---|---|---|---|---|---|
| 2025/11/01 | 10:00-13:00 | 山田 太郎 | 12345678 | 受付 | 特になし |

**② 場所一覧シート（場所IDの管理）**

| 場所ID | 場所名 |
|---|---|
| uketsuke | 受付 |
| stage | ステージ |

- QRコードのURLパラメータ（`?location=uketsuke`）と照合
- 場所の追加・変更はこのシートを編集するだけでよい

**③ 当日コードシート**

| 日付 | ログインコード |
|---|---|
| 2025/11/01 | 1234 |

### ● GAS機能

- シフトデータのJSON返却API（フロントからfetchで取得）
- 当日コードの照合API
- 学籍番号のシフト存在チェックAPI
- 18:30一括退勤のタイマートリガー（または呼び出しAPI）

---

## ■ 通知機能（Discord）

### ● 方法

- Discord Webhook使用

### ● 通知内容

- 出勤ログ（場所・時刻・学籍番号）
- 緊急連絡
- ※自動退勤はDiscord通知なし（サイレント処理）

---

## ■ 技術構成（推奨）

| 役割 | 技術 |
|---|---|
| フロントエンド | HTML / JS（Webアプリ） |
| バックエンド | Firebase または Supabase |
| シフト管理 | Google Apps Script（GAS） |
| 通知 | Discord Webhook |

---

## ■ データ構造（参考）

### 打刻レコード（出退勤テーブル）

| フィールド | 型 | 備考 |
|---|---|---|
| id | string | ユニークID |
| student_id | string | 学籍番号 |
| location_id | string | 場所ID |
| check_in_at | timestamp | 出勤時刻 |
| check_out_at | timestamp | 退勤時刻（自動退勤・一括退勤で記録） |
| date | date | 当日の日付 |

### 迷子ログテーブル

| フィールド | 型 | 備考 |
|---|---|---|
| id | string | ユニークID |
| student_id | string | 学籍番号 |
| scanned_location_id | string | スキャンした（誤った）場所ID |
| scanned_at | timestamp | スキャン時刻 |
| date | date | 当日の日付 |

---

## ■ 割り切り仕様

- 権限管理なし（管理者機能は今後の拡張）
- 打刻ミス修正機能なし
- アカウント登録機能なし
- 退勤ボタンなし（自動退勤・一括退勤のみ）
- 出勤状況のリアルタイム更新なし（再読み込みで対応）

---

## ■ バリデーション

- 学籍番号の形式バリデーション（桁数・数字のみ等）

---

## ■ 今後の拡張案

- 管理者権限・管理者ダッシュボード
- 打刻履歴の編集機能（管理者向け）
- 出勤状況のリアルタイム更新（WebSocket等）
- 通知の細分化
- 打刻ミス修正機能
